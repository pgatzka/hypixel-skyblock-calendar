spring.application.name=hypixel-skyblock-calendar

# Flyway owns the schema; Hibernate only validates that the entity matches it.
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false
spring.flyway.enabled=true

# Datasource is supplied per environment (env vars in prod; @DynamicPropertySource in tests).
